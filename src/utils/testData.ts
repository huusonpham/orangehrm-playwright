export const creds = {
  username: process.env.USERNAME || 'Admin',
  password: process.env.PASSWORD || 'admin123'
};

export const queries = {
  menuSearch: process.env.SEARCH_TERM || 'PIM',
  nonsense: 'zzzzzz'
};
